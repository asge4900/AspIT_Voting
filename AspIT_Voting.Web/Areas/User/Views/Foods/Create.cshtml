@model CreateFoodViewModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <a asp-action="Index">Tilbage</a>
</div>

<h1>Opret</h1>

<h4>Fredagens ret</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="FoodName" list="foods" class="form-control" autocomplete="off" />
                <datalist id="foods">
                    @foreach (var item in Model.FoodSuggestionsList)
                    {
                        <option value="@item.Value">@item.Text</option>
                    }
                </datalist>
                <span asp-validation-for="FoodName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Opret" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


@*<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">                
                <select asp-for="FoodName" asp-items="@Model.FoodSuggestionsList" class="form-control">
                    <option value="" hidden disabled selected>Vælg Ret</option>
                </select>
                <span asp-validation-for="FoodName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Opret" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>*@

<div>
    <a asp-action="Index">Tilbage</a>
</div>