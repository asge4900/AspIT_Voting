@model List<ActivityViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ActivityName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.VoteCount)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreationDate)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ActivityName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.VoteCount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreationDate)
                    </td>
                    <td>
                            <a asp-action="Edit" asp-route-id="@item.ActivityId">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.ActivityId">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.ActivityId">Delete</a>
                        </td>
                </tr>
            }
        </tbody>
    </table>*@

@for (int i = 0; i < Model.Count; i++)
{
    <div class="row">
        <form asp-action="Vote">
            <div class="form-check">
                <input type="hidden" asp-for="@Model[i].ActivityId" />
                <input type="hidden" asp-for="@Model[i].ActivtyName" />
                <input type="hidden" name="Index" value="@i" /> @*Multiple forms submit*@
                <input asp-for="@Model[i].IsThumbsUp" onChange="this.form.submit()" class="form-check-input thumbsUpInput" />
                <label asp-for="@Model[i].IsThumbsUp" class="thumbsUpLabel">
                    <img src="~/images/thumbs-up-solid.svg" alt="thumbsUp"/>
                </label>
            </div>
        </form>
        <div>
            @Model[i].ActivtyName
        </div>
        <div style="margin-left: 10px;">
            @Model[i].VoteCount
        </div>
    </div>
}






